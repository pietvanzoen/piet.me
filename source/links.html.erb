---
title: Links
description: Links worth sharing.
changefreq: daily

---

<h1>Links</h1>

<p class="intro-text">Excellent things from around the web.</p>

<hr>

<% data.links.items.each do |article| %>
<article>
  <header>
    <h2><a rel="external" href="<%= article['link'] %>"><%= escape_html article['title'] %></a></h2>
  </header>
  <% if article['excerpt'] %>
    <blockquote><p><%= escape_html article['excerpt'] %></p></blockquote>
  <% end %>

  <%
  @tags = article['tags'].select{ |t| t != 'share'}
  unless @tags.empty? %>
  <p class="article-meta"><small><a href="<%= article['link'] %>" rel="external"><%= article['domain'] %></a> | Tags: <%= @tags.join(', ') %></small></p>
  <% end %>
  <hr>
</article>
<% end %>
