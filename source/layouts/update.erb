
<%
    @title = "Update: \"#{truncate(strip_tags( current_article.data.html ), :length => 72).strip}\""
    @description = strip_tags(current_article.data.html).strip
%>

<% wrap_layout :layout do %>

  <%= partial(:update_item, :locals => { :update => current_article.data }) %>
  <%= partial(:comments) %>

<% end %>
