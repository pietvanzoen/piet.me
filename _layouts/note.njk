---
layout: layout.njk

---

<article class="h-entry">
  <h1 class="headline p-name">{{ title }}</h1>
  {% if unsplashImageId %}
  {%- HeroImage unsplashImageId, imageAlt or title, heroStyle -%}
  {% endif %}
  {% if wip %}
  <div class="card bg-offset">
    <strong>👷 Work in progress</strong>
    <p>This page is particularly fresh and actively being worked on. Please be kind.</p>
  </div>
  {% endif %}
  <div class="e-content">{{ content | safe }}</div>
  <hr>

  <aside class="card">
    <em>Did you find this valuable? <a href="https://buymeacoffee.com/pietvanzoen">Please consider contributing to my book fund</a>. ❤️</em>
  </aside>

  {% include "meta.njk" %}
</article>

{% include "backlinks.njk" %}

{% include "webmentionlist.njk" %}

{% include "comments.njk" %}
