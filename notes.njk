---
layout: layout.njk
title: Notes

---
<h1 class="headline">Notes</h1>

{%- for post in collections.notes | reverse -%}

<div class="buffer-md">
  <h2><a href="{{post.url}}">{{ post.data.title }}</a></h2>
  {% if post.data.excerpt %}
  {{ post.data.excerpt | markdown | safe }}
  {% endif %}
  {%- if post.data.tags %} <small class="text-muted">tags: {{ post.data.tags | join(', ') }}</small>{%- endif -%}
</div>
<hr>
{%- endfor -%}

<h2>Explore tags</h2>

<ul>
{% for tag in collections.tagList %}
  <li><a href="/tags/{{ tag | slug }}">{{ tag | title}}</a></li>
{% endfor %}
</ul>
