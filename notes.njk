---
layout: layout.njk
title: Archive

---
<h1 class="headline">Archive</h1>

<ul>
{%- for post in collections.notes | reverse -%}
<li><a href="{{post.url}}">{{ post.data.title }}</a> {%- if post.data.tags %} <small class="text-muted">tags: {{ post.data.tags | join }}</small>{%- endif -%}</li>
{%- endfor -%}
</ul>
